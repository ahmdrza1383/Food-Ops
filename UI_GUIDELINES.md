# دستورالعمل‌های جامع طراحی رابط و تجربه کاربری (UI/UX Guidelines) - پروژه FoodOps

## ۱. معرفی کلی و استراتژی طراحی (Overview & Strategy)

پروژه **FoodOps** یک سیستم جامع مدیریت سفارش و عملیات رستوران است. هدف اصلی UI ایجاد تجربه‌ای روان، سریع و اشتهاآور برای مشتریان و ابزاری کارآمد، پرسرعت و بدون خطا برای پرسنل (آشپزخانه، صندوق‌دار و مدیر) است.

طراحی سمت مشتری کاملاً **"Mobile-First"** است، در حالی که پنل‌های آشپزخانه و صندوق‌دار برای نمایشگرهای بزرگ‌تر و صفحات لمسی (Touch Display) بهینه‌سازی می‌شوند.

---

## ۲. پشته تکنولوژی (Tech Stack)

* **فریم‌ورک:** React.js یا Vue.js (معماری کامپوننت‌محور و Reusable).
* **استایل‌دهی:** Tailwind CSS (برای حفظ یکپارچگی سرعت توسعه و کلاس‌های کاربردی).
* **آیکون‌ها:** Lucide React/Vue یا Heroicons.
* **فونت:** Vazirmatn (وزیرمتن) - پشتیبانی کامل از اعداد فارسی و خوانایی بالا.

---

## ۳. اصول بصری و پالت رنگی (Visual Design)

### الف) پالت رنگی اصلی (Brand Palette)

* **رنگ اصلی (Primary):** نارنجی گرم (`#FF6B00` / `tailwind: orange-600`) برای دکمه‌های اصلی اقدام (CTA) مانند "ثبت سفارش" و "افزودن به سبد".
* **رنگ ثانویه (Secondary):** سبز زیتونی/تیره (`#2D6A4F` / `tailwind: emerald-800`) برای تأییدها و گزینه‌های سلامت/گیاهی.
* **پس‌زمینه (Background):** سفید خالص (`#FFFFFF`) یا خاکستری روشن (`#F8F9FA` / `tailwind: gray-50`).
* **متن (Text):** خاکستری تیره (`#1D3557` / `tailwind: slate-800`) جهت خوانایی بالا. **(از مشکی خالص `#000000` استفاده نشود)**.

### ب) نشانگرهای وضعیت سفارش (Status Badges)

برای تشخیص سریع وضعیت سفارش‌ها توسط مشتری و پرسنل، رنگ‌های زیر استاندارد شده‌اند:

| وضعیت (Status) | کلید دیتابیس | رنگ اصلی | کلاس Tailwind پیشنهادی |
| --- | --- | --- | --- |
| **ثبت‌شده** | `registered` | زرد هشدار | `bg-amber-100 text-amber-800 border-amber-300` |
| **در حال آماده‌سازی** | `preparing` | نارنجی | `bg-orange-100 text-orange-800 border-orange-300` |
| **آماده تحویل** | `ready_for_delivery` | آبی | `bg-sky-100 text-sky-800 border-sky-300` |
| **تحویل داده‌شده** | `delivered` | سبز موفقیت | `bg-emerald-100 text-emerald-800 border-emerald-300` |
| **لغوشده** | `canceled` | قرمز | `bg-rose-100 text-rose-800 border-rose-300` |

### ج) تایپوگرافی (Typography)

* **فونت پایه:** Vazirmatn.
* **وزن‌ها:**
* عناوین اصلی و قیمت‌های کلیدی: Bold (700)
* عناوین کارت‌ها و دکمه‌ها: Semi-Bold (600)
* متن بدنه و توضیحات: Regular (400)


* **اندازه فونت:** سایز پایه برای موبایل حداقل `16px` جهت جلوگیری از زوم ناخواسته در iOS.

### د) تصاویر و بارگذاری (Imagery & Skeleton)

* نسبت تصویر کارت محصول: `4:3` یا `1:1`.
* انحنای زوایا: `border-radius: 12px` (`rounded-xl`).
* **Skeleton Loader:** هنگام بارگذاری داده‌ها یا تصاویر، حتماً از Skeleton متحرک با شیمر (Shimmer) استفاده شود، نه اسپینر چرخشی.

---

## ۴. نقاط شکست و واکنش‌گرایی (Responsive Breakpoints)

طراحی باید روی همه دستگاه‌ها بر اساس نقاط شکست زیر تنظیم شود:

* **Mobile (`< 640px`):**
* چیدمان لیست منو: ۱ ستونه.
* سبد خرید: کشوی بازشونده از پایین (Bottom Sheet).


* **Tablet (`640px - 1024px`):**
* چیدمان لیست منو: ۲ ستونه.
* پنل آشپزخانه: ۲ ستونه.


* **Desktop (`> 1024px`):**
* چیدمان منو: ۳ یا ۴ ستونه.
* سبد خرید: کشوی ثابت یا Side-Drawer از سمت راست.



---

## ۵. کامپوننت‌های کلیدی سمت مشتری (Customer UI)

### الف) کارت محصول (Product Card)

* شامل: تصویر با کیفیت، نام غذا (Bold)، توضیحات کوتاه (حداکثر ۲ خط با `line-clamp-2`)، قیمت با جداکننده سه رقمی و دکمه شناور `+` جهت افزودن سریع به سبد.
* افکت Hover (دسکتاپ): بزرگ‌نمایی نرم (`scale-105`) و سایه ملایم (`shadow-lg`).

### ب) نوار دسته‌بندی شناور (Sticky Category Bar)

* موقعیت: چسبیده به بالای صفحه (`sticky top-0`) با زِد-اندکس بالا.
* دارای اسکرول افقی نرم (`overflow-x-auto`) در موبایل.
* دسته‌بندی فعال با رنگ اصلی (`Primary`) و خط زیرین برجسته مشخص شود.

### ج) سبد خرید تعاملی (Cart Drawer)

* آیکون سبد خرید همیشه حاوی تعداد آیتم‌ها (Badge) در هدر یا گوشه پایین صفحه شناور باشد.
* کلیک روی سبد خرید باعث باز شدن کشو (Drawer) می‌شود بدون اینکه کاربر صفحه منو را ترک کند.

### د) فرم‌ها و اعتبارسنجی (Forms & Validation)

* ورودی شماره تلفن همراه باید اعتبارسنجی لحظه‌ای (Real-time) فرمت ایران (`^09\d{9}$`) داشته باشد.
* نمایش پیام‌های خطا با رنگ قرمز ملایم و آیکون هشدار در زیر همان Input.

### هـ) حالت‌های خالی و قطعی شبکه (Empty States & Offline)

* **سبد خرید خالی:** نمایش تصویر/تصویرسازی ظرف خالی + دکمه "مشاهده منو و سفارش".
* **عدم وجود سابقه سفارش:** نمایش پیام راهنما جهت ثبت اولین سفارش.
* **قطعی اینترنت:** نمایش نوار هشدار ثابت (Alert Banner) در بالای صفحه با متن "ارتباط شما با شبکه قطع شده است."

---

## ۶. پنل‌های تخصصی و نقش‌ها (Multi-Role Panels UI)

### الف) پنل آشپزخانه (Kitchen Display System - KDS)

* **تم و کنتراست:** دارای حالت Dark Mode یا کنتراست بالا جهت خوانایی از فاصله چند متری در محیط آشپزخانه.
* **تایمر زمان آماده‌سازی (Prep Timer):** نمایش زمان گذشته از ثبت سفارش روی هر کارت با تغییر رنگ:
* کمتر از ۱۰ دقیقه 👈 سبز
* ۱۰ تا ۲۰ دقیقه 👈 زرد
* بیشتر از ۲۰ دقیقه 👈 قرمز هشدار


* **کلیدهای لمسی بزرگ:** دکمه‌های "شروع آماده‌سازی" (`/start`) و "آماده تحویل" (`/ready`) باید جهت کار با مانیتورهای لمسی حداقل ارتفاع `48px` داشته باشند.

### ب) پنل صندوق‌دار / تحویل (Cashier View)

* نمایش کارت‌های سفارشات آماده تحویل (`ready_for_delivery`).
* قابلیت **جستجوی سریع** بر اساس شماره سفارش یا شماره تلفن مشتری در بالای صفحه.
* دکمه برجسته "ثبت تحویل نهایی" (`/deliver`) با دیالوگ تأیید سریع.

---

## ۷. انیمیشن‌ها و تعاملات زنده (Micro-interactions)

* **افزودن به سبد:** انیمیشن پرتابی یا جابجایی کوتاه آیکون غذا به سمت آیکون سبد خرید.
* **اعلان‌ها (Toast Notifications):** استفاده از نوار اعلان شناور برای بازخوردهای مهم (مثلاً "سفارش شما با موفقیت لغو شد").
* **تغییر وضعیت زنده:** استفاده از CSS Transition هنگام تغییر وضعیت کارت‌های سفارش (مثلاً تغییر نرم رنگ Badge از زرد به نارنجی).

---

## ۸. نکات امنیتی و فنی در فرانت‌اند (Technical & Security UI)

* **مدیریت توکن و انقضا:** در صورت دریافت خطای `401 Unauthorized` از سرور، توکن JWT پاک شده، یک Modal با پیام "جلسه کاری شما به پایان رسیده است" نمایش داده شود و کاربر به صفحه لاگین هدایت گردد.
* **مدیریت محدودیت درخواست (Rate Limit - 429):** در صورت دریافت خطای `429` پیام "تعداد درخواست‌های شما بیش از حد مجاز است. لطفاً چند لحظه صبر کنید" نمایش داده شود.
* **مدیریت خطاهای سراسری (Error Boundary):** استفاده از Error Boundary در React/Vue تا در صورت رخ دادن باگ ناگهانی، کل صفحه سفید نشود و یک فرم بازگردانی نمایش داده شود.

---

## ۹. دسترسی‌پذیری (Accessibility - a11y)

* تمام دکمه‌های آیکونی (مانند دکمه حذف، بسته شدن کشو و دکمه `+`) باید دارای صفت `aria-label` باشند.
* کنتراست رنگ متن‌ها با پس‌زمینه باید حداقل استاندارد **WCAG AA** (نسبت کنتراست ۴.۵ به ۱) را رعایت کند.