# 🎨 راهنمای طراحی و معماری فرانت‌اند پروژه FoodOps

این سند به عنوان مرجع اصلی برای توسعه‌دهندگان فرانت‌اند پروژه FoodOps تدوین شده است تا هماهنگی کامل بین رابط کاربری و معماری Backend (Node.js/Express + MongoDB) تضمین شود.

---

## ۱. تکنولوژی‌ها و معماری کلی (Tech Stack)

بر اساس نیازمندی‌های پروژه، تمرکز بر سادگی، خوانایی و پرهیز از پیچیدگی‌های غیرضروری است:

* **هسته اصلی:** HTML5, CSS3, JavaScript (ES6+ Modules)
* **باندلر (توصیه شده):** استفاده از ابزارهای سبک مانند **Vite** برای مدیریت ماژول‌ها و اجرای محیط توسعه.
* **استایل‌دهی:** **Tailwind CSS** (برای پیاده‌سازی سریع، یکپارچه و واکنش‌گرا).
* **ارتباط با سرور:** استفاده از `Fetch API` بومی جاوااسکریپت.
* **مدیریت وضعیت (State Management):** استفاده از `localStorage` برای ذخیره توکن (JWT) و سبد خرید (Cart).

---

## ۲. ساختار پوشه‌بندی (Directory Structure)

برای جلوگیری از درهم‌ریختگی کدهای خام JS و HTML، ساختار ماژولار زیر الزامی است:

```text
frontend/
├── index.html              # 👈 نقطه ورود اصلی سایت (اصلی /)
├── package.json            # مدیریت پکیج‌ها
├── vite.config.js          # تنظیمات باندلر
├── tailwind.config.js      # تنظیمات Tailwind CSS
│
└── src/
    ├── assets/             # فایل‌های CSS پایه و فونت‌ها
    ├── components/         # اجزای تکرارشونده UI (Header, FoodCard, CartDrawer و...)
    ├── pages/              # سایر صفحات و منطق مربوط به نقش‌ها
    │   ├── auth/           # login.html, register.html
    │   ├── customer/       # checkout.html, my-orders.html
    │   ├── kitchen/        # dashboard.html
    │   ├── delivery/       # dashboard.html
    │   └── admin/          # dashboard.html, menu-manager.html, reports.html
    ├── services/           # توابع ارتباط با API (api.js, auth.js, order.js)
    └── utils/              # توابع کمکی (formatPrice, validators و...)
```

---

## ۳. لیست صفحات و مسیرهای کاربری (UI Pages & Routes)

پروژه بر اساس **RBAC (کنترل دسترسی مبتنی بر نقش)** به ۴ بخش اصلی تقسیم می‌شود:

### الف) بخش عمومی، صفحه اصلی و احراز هویت (Auth & Public)

* **صفحه اصلی و منوی غذاها (`/`):**
* شامل **هدر اصلی سایت (Header)** با دسترسی به ورود/ثبت‌نام، پروفایل و دکمه سبد خرید.
* نمایش **دسته‌بندی‌های منو** (فیلتر بر اساس گروه غذاها).
* نمایش **کارت‌های غذا (Food Cards)** شامل عکس، نام، قیمت، توضیحات، وضعیت موجودی و دکمه «افزودن به سبد خرید».
* **سبد خرید کشویی/مودال (Cart Drawer/Modal):** مشاهده کشویی یا کشوی شناور سبد خرید، امکان تغییر تعداد آیتم‌ها و نمایش جمع کل سفارش.


* **صفحه ورود (`/login`):** دریافت شماره موبایل و رمز عبور.
* **صفحه ثبت‌نام (`/register`):** دریافت نام، شماره موبایل و رمز عبور.

> **الزام:** اعتبارسنجی فرانت‌اند (Frontend Validation) برای صحت فرمت شماره موبایل (شروع با ۰۹) و طول رمز عبور (حداقل ۶ کاراکتر) پیش از ارسال درخواست الزامی است.

---

### ب) پنل مشتری (Customer)

* **صفحه ثبت نهایی (`/checkout`):** مرور سفارش، وارد کردن آدرس/توضیحات و اعمال کد تخفیف.
* **صفحه تاریخچه سفارشات (`/my-orders`):** لیستی از سفارشات کاربر با امکان فیلتر و مشاهده وضعیت لحظه‌ای (پیگیری آنلاین).

---

### ج) پنل آشپزخانه (Kitchen Staff)

* **داشبورد صف سفارشات (`/kitchen`):** ترجیحاً طراحی به صورت **Kanban Board** با دو ستون اصلی:
1. **ثبت‌شده (New):** دکمه تغییر وضعیت به «در حال آماده‌سازی».
2. **در حال آماده‌سازی (Preparing):** دکمه تغییر وضعیت به «آماده تحویل».



---

### د) پنل صندوق‌دار / مسئول تحویل (Cashier)

* **داشبورد تحویل (`/delivery`):** لیستی از سفارشات که در وضعیت «آماده تحویل» هستند. قابلیت جستجو بر اساس شناسه یا شماره موبایل مشتری و دکمه «تحویل نهایی و بستن سفارش».

---

### هـ) پنل مدیریت (Admin)

* **مدیریت منو و دسته‌بندی (`/admin/menu`):** فرم‌های CRUD برای ساخت، ویرایش و حذف دسته‌بندی‌ها و آیتم‌های غذا.
* **گزارشات (`/admin/reports`):** نمایش داشبورد آماری شامل پرفروش‌ترین آیتم‌ها و حجم سفارشات روزانه.
* **مدیریت کاربران (`/admin/users`):** تغییر نقش کاربران (مثلاً ارتقای مشتری به پرسنل آشپزخانه یا پیک).

---

## ۴. سیستم طراحی و تجربه کاربری (UI/UX System)

رابط کاربری باید ساده، خوانا و دارای **تمایز شفاف در نمایش وضعیت‌ها** باشد.

### رنگ‌بندی وضعیت‌های سفارش (Status Badges)

سفارشات باید در تمامی پنل‌ها با لیبل‌های رنگی مجزا نمایش داده شوند:

* **ثبت‌شده (`registered`):** ⚪️ خاکستری یا آبی روشن (`bg-gray-100 text-gray-800`)
* **در حال آماده‌سازی (`preparing`):** 🟠 نارنجی (`bg-orange-100 text-orange-800`)
* **آماده تحویل (`ready_for_delivery`):** 🔵 آبی فیروزه‌ای (`bg-cyan-100 text-cyan-800`)
* **تحویل‌شده (`delivered`):** 🟢 سبز (`bg-green-100 text-green-800`)
* **لغوشده (`canceled`):** 🔴 قرمز (`bg-red-100 text-red-800`)

### اجزای بصری کلیدی

* **پیام‌های سیستمی (Toast/Alert):** تمامی عملیات‌ها (موفقیت در ثبت، خطای موجودی، خطای ورود) باید با پیام‌های واضح به کاربر نمایش داده شوند.
* **وضعیت موجودی:** اگر `stock_quantity` برابر صفر بود، دکمه افزودن به سبد خرید باید `Disabled` شده و لیبل "ناموجود" با رنگ قرمز نمایش داده شود.

---

## ۵. ملاحظات امنیتی و ادغام با API (بسیار مهم)

ارتباط فرانت‌اند با بک‌اند باید با رعایت دقیق پروتکل‌های امنیتی زیر انجام شود:

1. **توکن احراز هویت (JWT):** پس از لاگین موفق، توکن دریافتی در `localStorage` ذخیره شود. تمامی درخواست‌ها (به جز عضویت و ورود) باید هدر زیر را داشته باشند:
`Authorization: Bearer <TOKEN>`
2. **محاسبه قیمت (قانون طلایی):** **به هیچ‌وجه** قیمت نهایی یا مجموع سبد خرید نباید از سمت فرانت‌اند به بک‌اند ارسال شود. سبد خرید ارسالی در متد `POST /api/orders` **فقط و فقط** باید شامل آیدی آیتم‌ها (`menu_item_id`) و تعداد (`quantity`) باشد. سرور قیمت‌ها را از دیتابیس خوانده و محاسبه می‌کند.
3. **مدیریت خطای ۴۰۱ و ۴۰۳:** یک `Interceptor` (یا تابع Wrapper دور Fetch) پیاده‌سازی شود تا در صورت دریافت خطای `401 Unauthorized` (انقضای توکن)، کاربر فوراً لاگ‌آوت شده و به صفحه `/login` هدایت شود.
4. **جلوگیری از دستکاری DOM:** مخفی کردن دکمه‌ها در HTML کافی نیست. در صورت دستکاری DOM، بک‌اند به دلیل بررسی نقش (RBAC) ارور `403 Forbidden` خواهد داد و فرانت‌اند باید این خطاها را به‌درستی دریافت و مدیریت کند.
5. **خروج ایمن:** هنگام فراخوانی API لاگ‌اوت (`POST /api/auth/logout`)، توکن و اطلاعات جلسات کاری باید به طور کامل از `localStorage` پاک شوند.
