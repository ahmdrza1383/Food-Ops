# دستورالعمل‌های طراحی رابط کاربری (UI/UX) - پروژه FoodOps

## ۱. معرفی کلی و هدف
پروژه **FoodOps** یک سیستم مدیریت سفارش و رستوران است. هدف اصلی UI، ایجاد تجربه‌ای سریع، اشتهاآور و بدون اصطکاک برای مشتریان و کارکنان رستوران است. طراحی باید "Mobile-First" باشد زیرا اکثر سفارش‌ها از طریق موبایل ثبت می‌شوند.

## ۲. پشته تکنولوژی (Tech Stack)
- **فریم‌ورک:** React.js یا Vue.js (بسته به انتخاب تیم، اما ساختار کامپوننت‌محور الزامی است).
- **استایل‌دهی:** Tailwind CSS (ترجیحی) یا CSS Modules.
- **آیکون‌ها:** Heroicons یا Lucide React/Vue.
- **فونت:** Vazirmatn (وزیرمتن).

## ۳. اصول بصری و طراحی (Visual Design)

### الف) پالت رنگی (Color Palette)
رنگ‌ها باید حس تازگی و گرما را منتقل کنند:
- **رنگ اصلی (Primary):** نارنجی گرم (`#FF6B00`) یا قرمز گوجه‌ای (`#E63946`) برای دکمه‌های اقدام (CTA) مثل "افزودن به سبد".
- **رنگ ثانویه (Secondary):** سبز تیره (`#2D6A4F`) برای نشان دادن وضعیت "آماده" یا گزینه‌های گیاهی/سالم.
- **پس‌زمینه (Background):** سفید خالص (`#FFFFFF`) یا خاکستری بسیار روشن (`#F8F9FA`) برای تمیز نشان دادن عکس غذاها.
- **متن (Text):** خاکستری تیره (`#1D3557`) برای خوانایی بالا. هرگز از مشکی خالص (`#000000`) استفاده نکنید.

### ب) تایپوگرافی (Typography)
- از فونت **Vazirmatn** استفاده شود.
- وزن‌های پیشنهادی:
  - عناوین اصلی: Bold (700)
  - عناوین فرعی و قیمت‌ها: Semi-Bold (600)
  - توضیحات و متن بدنه: Regular (400)
- سایز فونت پایه برای موبایل حداقل 16px باشد.

### ج) تصاویر (Imagery)
- عکس‌ها قهرمان صفحه هستند. از نسبت تصویر 4:3 یا 1:1 برای کارت‌های محصول استفاده شود.
- تمام عکس‌ها باید دارای `border-radius: 12px` باشند.
- هنگام لود شدن عکس، از **Skeleton Loader** استفاده شود (نه اسپینر چرخشی).

## ۴. کامپوننت‌های کلیدی و رفتار آن‌ها

### الف) کارت محصول (Product Card)
- شامل: عکس بزرگ، نام غذا (بولد)، توضیحات کوتاه (حداکثر ۲ خط)، قیمت (رنگ متمایز)، دکمه "+" شناور در گوشه پایین.
- افکت Hover: کمی بزرگنمایی (Scale 1.05) و سایه نرم.

### ب) نوار دسته‌بندی (Category Sticky Bar)
- در بالای لیست منو ثابت بماند (Sticky).
- اسکرول افقی داشته باشد.
- آیتم فعال با رنگ اصلی و زیرخط مشخص شود.

### ج) سبد خرید (Cart Interaction)
- آیکون سبد خرید همیشه در هدر یا گوشه پایین صفحه شناور باشد.
- با کلیک روی آن، یک **Drawer** (منوی کشویی) از سمت راست (در دسکتاپ) یا پایین (در موبایل) باز شود.
- کاربر نباید برای دیدن سبد خرید از صفحه فعلی خارج شود.

### د) فرم‌ها و اعتبارسنجی (Forms & Validation)
- تمام ورودی‌ها باید لیبل واضح داشته باشند.
- اعتبارسنجی سمت کاربر (Frontend Validation) باید لحظه‌ای باشد (مثلاً فرمت شماره تماس).
- پیام‌های خطا با رنگ قرمز ملایم و آیکون هشدار نمایش داده شوند.

## ۵. نکات امنیتی و فنی (Security & Performance)
- **Helmet & CORS:** مطمئن شوید که هدرهای امنیتی توسط Backend تنظیم شده‌اند و Frontend فقط با دامنه‌های مجاز ارتباط برقرار می‌کند.
- **Rate Limiting:** در صورت دریافت خطای 429، یک پیام دوستانه به کاربر نمایش دهید: "لطفاً چند لحظه صبر کنید و دوباره تلاش کنید."
- **Authorize Middleware:** اگر توکن JWT منقضی شد، کاربر به صورت خودکار به صفحه لاگین هدایت شود و پیام "جلسه شما منقضی شده است" نمایش داده شود.
- **Error Handling:** از نمایش خطاهای خام سرور به کاربر خودداری کنید. از یک Error Boundary سراسری در React/Vue استفاده کنید.

## ۶. انیمیشن‌ها (Micro-interactions)
- افزودن به سبد خرید: انیمیشن پرواز آیکون به سمت سبد.
- تغییر وضعیت سفارش: استفاده از Transition‌های نرم برای تغییر رنگ وضعیت (مثلاً از زرد به سبز).

## ۷. دسترسی‌پذیری (Accessibility)
- تمام دکمه‌ها و لینک‌ها باید دارای `aria-label` مناسب باشند.
- کنتراست رنگی متن و پس‌زمینه باید استاندارد WCAG AA را رعایت کند.